{
  "name": "cursor-knowledge-management-system",
  "private": true,
  "type": "module",
  "devDependencies": {
    "markdown-link-check": "^3.12.2"
  },
  "scripts": {
    "skills:check": "node scripts/check-skill-structure.mjs",
    "commands:check": "node scripts/check-command-structure.mjs",
    "links:check": "node scripts/check-links.mjs",
    "docs:check": "npm run skills:check && npm run commands:check && npm run links:check",
    "release": "bash scripts/release.sh"
  }
}
